import React from "react";

const Footer = () => {
  const foot = {
    background: "#1E456E",
    position: "relative",
    bottom: "0",
    width: "100%",
    textAlign: "center",
    height: "5%"
  };

  return (
    <div style={foot}>
      <footer>
        <h2>ASSIGNMENT</h2>
      </footer>
    </div>
  );
};

export default Footer;
